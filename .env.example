# Application Configuration
# Copy this file to .env and fill in your values

# Required: JWT Secret for token signing (must be at least 32 characters)
JWT_SECRET=your-secret-key-must-be-at-least-32-characters-long-change-this-in-production

# Database Configuration
# Default: sqlite:data/terminal.db
DATABASE_URL=sqlite:data/terminal.db

# JWT Token Configuration
# Default: 24 hours
JWT_EXPIRATION_HOURS=24

# Logging
# Default: info (options: trace, debug, info, warn, error)
LOG_LEVEL=info

# Solana Configuration
# Network: mainnet, devnet, testnet (default: devnet)
SOLANA_NETWORK=devnet

# Solana RPC URL (optional, uses network default if not set)
# SOLANA_RPC_URL=https://api.devnet.solana.com

# Helius API Key (optional, for premium RPC access)
# HELIUS_API_KEY=your-helius-api-key-here

# AI Chat Configuration (optional - at least one provider key required for AI features)
# AI Provider API Keys (set at least one for AI chat to work)
# DEEPSEEK_API_KEY=your-deepseek-api-key
# OPENAI_API_KEY=your-openai-api-key
# ANTHROPIC_API_KEY=your-anthropic-api-key
# GEMINI_API_KEY=your-gemini-api-key

# AI Chat Settings (optional)
# AI_SYSTEM_PROMPT=You are a helpful assistant for the XForce Terminal.
# AI_MAX_TOKENS=4096
# AI_TEMPERATURE=0.7
# AI_CONTEXT_WINDOW=8192
